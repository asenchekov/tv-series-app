import React from 'react'

const Spinner = (props) => {
    return (
        <div id="loadingSVG">
            <img src='91.svg' alt='...LOADING' />
            <h2>loading...</h2>
        </div>
    );
};

export default Spinner;
